{
  "name": "pi-fixes",
  "version": "0.1.0",
  "description": "Bundled Pi workarounds (Codex SSE timeout, oversized-message guard) with built-in effectiveness tracking so each fix can be retired once it stops firing.",
  "private": true,
  "keywords": ["pi-package", "pi", "pi-extension", "codex", "transport", "sse", "context", "guard", "fixes"],
  "type": "module",
  "pi": {
    "extensions": [
      "./extensions/effectiveness.ts",
      "./extensions/codex-transport.ts",
      "./extensions/context-guard.ts",
      "./extensions/claude-effort.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "peerDependenciesMeta": {
    "@earendil-works/pi-coding-agent": {
      "optional": true
    }
  },
  "license": "MIT"
}
